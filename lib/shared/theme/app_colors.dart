import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary   = Color(0xFF1565C0); // Deep blue — Gulfwalkin brand
  static const secondary = Color(0xFF00897B); // Teal accent
  static const error     = Color(0xFFD32F2F);
  static const warning   = Color(0xFFF57C00);
  static const success   = Color(0xFF388E3C);

  static const background    = Color(0xFFF5F7FA);
  static const surface       = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFEEF2F7);

  static const textPrimary   = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint      = Color(0xFF9CA3AF);

  static const divider   = Color(0xFFE5E7EB);
  static const cardBorder = Color(0xFFE8ECF0);

  // Status badge colours
  static const statusPending    = Color(0xFFF59E0B);
  static const statusReviewed   = Color(0xFF3B82F6);
  static const statusShortlisted = Color(0xFF10B981);
  static const statusRejected   = Color(0xFFEF4444);
}
